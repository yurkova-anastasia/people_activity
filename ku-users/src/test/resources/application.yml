spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ku_users
    driver-class-name: org.postgresql.Driver
    username: ku_users_app
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: classpath:databases/changelog.xml